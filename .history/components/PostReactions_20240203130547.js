import { Button } from 'bootstrap';

function ReactionCard({ reactionObj }) {
  return (

    <Button variant="top" src={bookObj.image} alt={bookObj.title} style={{ height: '400px' }}
    </Button>
  );
}
